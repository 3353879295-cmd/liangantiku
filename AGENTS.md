# 项目协作规范

## Git 提交

- 每次改动完成后，都必须创建一个 Git commit，以便后续追踪和回滚。
- 每个 commit 应只包含本次改动相关的文件，避免混入无关修改。

## 测试与验证

- 每次改动后，都必须编写或更新与改动相关的测试。
- 交付前，必须运行相关测试和项目验证，并确认全部通过。

## 代码规范

- 遵循项目现有代码风格，保持实现清晰、简洁且易于维护。
- 采用模块化编程，让每个模块职责单一、边界明确，避免不必要的耦合。
