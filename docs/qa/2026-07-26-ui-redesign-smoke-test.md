# 2026-07-26 UI 重设计烟测记录

## 结论

全量自动验证和题库同步验证通过。微信开发者工具 CLI 可发现，但登录态探测和单次项目打开均超时且没有返回可判定结果；本轮没有可用的模拟器 GUI、GUI 自动化会话、iOS 真机或 Android 真机。因此开发者工具烟测和真机可读性检查全部为 `NOT RUN`，不计为通过。

记录时间：2026-07-26 18:12（Asia/Shanghai）

## 环境

- 工作树：`D:\文档\保管员刷题小程序\.worktrees\grain-practice-miniapp`
- 分支：`codex/grain-practice-miniapp`
- 小程序目录：`miniapp/`
- 微信开发者工具：`C:\Program Files (x86)\Tencent\微信web开发者工具`
- 可执行文件产品/文件版本：`1.03.0`
- CLI：`C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat`
- 探测前活动进程：未发现微信开发者工具相关进程
- 截图：未采集；没有把静态代码或自动化测试当作 GUI 截图证据

## 自动化执行记录

| 操作                          | 结果  | 观察到的证据                                                                                                                          |
| ----------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 新结构断言专项测试            | PASS  | `project-structure.test.ts` 17/17；覆盖三主 tab、16 个注册页面四件套、递归注册组件和本地图片素材。                                    |
| 首次 `npm run verify`         | RED   | TypeScript、ESLint、Stylelint 通过；Prettier 报 `tests/project-structure.test.ts` 格式不匹配，测试阶段未执行。                        |
| 格式修正后的 `npm run verify` | GREEN | exit 0；21 个测试文件、163 个测试全部通过。没有修改产品行为。                                                                         |
| `npm run sync:questions`      | PASS  | 六个分片输出 `20 / 8 / 9 / 8 / 8 / 8`，合计 61 道。                                                                                   |
| 同步后 `npm run test`         | PASS  | 21 个测试文件、163 个测试全部通过。                                                                                                   |
| 生成物稳定性                  | PASS  | `miniprogram/data/knowledge-catalog.ts` 与 `miniprogram/data/questions/` 的 `git diff --exit-code` 为 0；没有生成 diff 或 ID 不稳定。 |
| `git diff --check`            | PASS  | exit 0，无空白错误。                                                                                                                  |

题库同步期间 Git 输出“LF 将在未来 Git 触碰时替换为 CRLF”的工作区提示，但 `git status` 没有题库生成物变更，内容 diff 为 0。本轮不把行尾提示误报为 ID 漂移。

## 微信开发者工具 CLI

| 操作                                                              | 状态         | 精确结果                                                                                      |
| ----------------------------------------------------------------- | ------------ | --------------------------------------------------------------------------------------------- |
| `cli.bat --help`                                                  | PASS         | exit 0，列出 `open`、`islogin`、`build-npm`、`preview`、`upload` 等命令。                     |
| `cli.bat islogin --lang zh`                                       | INCONCLUSIVE | 34 秒无输出后超时；不能判定已登录或未登录。                                                   |
| `cli.bat open --project <工作树>\miniapp --lang zh --disable-gpu` | INCONCLUSIVE | 单次调用 64 秒无输出后超时；调用结束后未发现开发者工具进程，`git status` 也没有新增工程变更。 |
| `cli.bat build-npm`                                               | NOT RUN      | CLI 服务未能通过前述探测/打开启动；为避免反复驱动 GUI，本轮未继续调用。                       |
| 开发者工具编译与控制台检查                                        | NOT RUN      | 没有建立可用的开发者工具会话，不能证明编译或控制台无错误。                                    |

## 模拟器日间/夜间烟测

以下项目全部是微信开发者工具内的验收项。本轮没有可用 GUI 会话，因此每一行都是 `NOT RUN`；“自动化旁证”只帮助定位手工步骤，不将该行提升为通过。

| 状态    | 验收项                                                              | 自动化旁证与手工步骤                                                                   |
| ------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| NOT RUN | 首页两个职业各显示五等级，技师/高级技师显示“待补充”且不能开始空练习 | 自动化已覆盖可用性门禁。手工分别选择两个职业，逐个点击五等级，确认待补充项无练习跳转。 |
| NOT RUN | 首页只有章节刷题、随机练习、模拟考试、错题本、收藏试题五个功能      | 自动化已覆盖 Presenter/结构。手工数清五张入口卡并逐一打开。                            |
| NOT RUN | 教材目录章节编号为 1、2、3，无 01、02、03                           | 自动化已覆盖目录格式。手工切换职业等级后进入目录，检查所有章节标题。                   |
| NOT RUN | 单选、多选、判断、案例题的点击、切换、确认、正确和错误状态均正确    | 状态机测试已通过。手工为四种题型各完成一次正确与错误提交，并观察选项动效。             |
| NOT RUN | 模拟考试交卷前不泄露答案                                            | 会话测试已通过。手工进入模拟考试，作答并切题，确认交卷前无答案/解析。                  |
| NOT RUN | 收藏按钮支持收藏和取消，反馈约 1.6 秒自动隐藏                       | 收藏服务和 1600ms 定时器测试已通过。手工连续收藏/取消并计时观察。                      |
| NOT RUN | 夜间主题在退出页面、关闭并重开小程序后仍保留                        | ThemeService 测试已通过。手工切换夜间，返回、关闭小程序并重开三次观察。                |
| NOT RUN | 答题、答题卡、结果、解析、错题/收藏页面夜间颜色一致                 | 结构测试已覆盖主题组件接线。手工逐页检查背景、卡片、正文、状态色和按钮。               |
| NOT RUN | 实操图标无文字替代，详情稻穗照片本地加载                            | 素材存在性测试已通过。手工断网后打开实操列表与详情，确认图片仍渲染。                   |
| NOT RUN | 二级页均可返回，直接打开二级页时回到正确主 tab                      | 导航 Presenter 测试已通过。手工正常进入与直接编译打开每个二级页，分别检查返回目标。    |
| NOT RUN | “我的”没有重复的大面积复习板块                                      | 结构测试已通过。手工进入“我的”，检查版块与所有个人中心二级入口。                       |

## 真机可读性

| 状态    | 平台    | 必检项                                                                                               |
| ------- | ------- | ---------------------------------------------------------------------------------------------------- |
| NOT RUN | iOS     | 未提供或连接设备；需检查选项 `28rpx`、行高 1.5、字重 600、日夜对比度、触控高度、安全区和系统大字号。 |
| NOT RUN | Android | 未提供或连接设备；除同项检查外，需特别观察低端机字形是否粘连。                                       |

若低端 Android 字形粘连，只允许在 Android 样式分支把字重降至 `500`；记录机型、系统版本、开发者工具基础库版本和前后截图。未观察到真机现象前不得预防性修改，也不得把字重降至 `400`。

## 手工续验步骤

1. 登录微信开发者工具，导入本工作树的 `miniapp/`，记录工具版本和基础库版本。
2. 执行“工具 → 构建 npm”，再点击“编译”；保存控制台无错误、无未处理 Promise 的证据。
3. 在日间和夜间各按上表完整执行一次模拟器清单，并记录实际结果；失败项附页面、复现步骤和截图。
4. 分别在一台 iOS 和一台 Android 上预览，完成可读性、安全区和大字号检查。
5. 只有实际执行且观察符合预期的行才能从 `NOT RUN` 改为 `PASS`。

## 未解决关注项

- 实操测试允许既定本地素材，但没有锁定每个技能到图标的逐项语义映射；需在模拟器人工核对技能含义与图片。
- 薄弱章节指标完全相同时采用字典序而非数值序打破平局；本轮未改变或标记为已解决。
