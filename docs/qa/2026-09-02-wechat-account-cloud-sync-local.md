# 微信账号与云端学习同步本地 QA 记录

日期：2026-09-02

工作树：`D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification`

## 范围与约束

- 仅验证本地代码、配置和自动化测试。
- 云环境 `cloud1-d2gglad830c91db10` 尚未进行集合、权限、索引或云函数写入。
- 开始任务时工作树已有大量未提交修改；这些内容全部按用户内容保留，不纳入本功能提交。
- 本功能各阶段使用精确文件暂存和独立提交，未使用 `git add .`、`git add -A`、重置或覆盖命令。

## 分阶段提交

| 阶段         | 提交      | 内容                                        |
| ------------ | --------- | ------------------------------------------- |
| 计划         | `a671b05` | 详细实施计划与 TDD 阶段拆分                 |
| 存储隔离     | `1caa230` | 游客/账号命名空间与旧数据只迁移到游客       |
| 云函数客户端 | `28be8a6` | 固定环境初始化、白名单请求和稳定错误映射    |
| 云函数基础   | `443effa` | 可信身份、事务、资料/设置/会话/收藏/掌握    |
| 可靠变更     | `9afd87c` | 练习幂等、清除与注销可恢复状态机            |
| 同步队列     | `cf8448c` | 持久化 outbox、revision 冲突和云端恢复      |
| 服务集成     | `8364d42` | AuthService、ProgressService 与生命周期集成 |
| 页面         | `74b18f8` | 首次登录、账号与数据、资料页同步状态        |
| 包边界       | `ff25ab6` | 8 个次级页面迁入辅助分包，保持主包预算      |
| 云存储修复   | `603d187` | NotFound、首练习读取和 `_id` 写回契约       |
| 响应上限     | `db17124` | 900 KiB 快照上限与越界事务回滚              |
| 恢复清除     | `f7a728b` | 无损重登和可跨重启续做的清除状态            |
| 数据库配置   | `7a1e900` | 数据库级 NotFound 配置与入口契约测试        |
| 清除竞态     | `5c3b23d` | 延迟资料同步，清除后 drain 并刷新内存       |

本记录与安全门禁在独立复验和最终审查完成后作为最后一个本地阶段提交。

## TDD 与验证证据

- 安全门禁首次运行：3 项中 2 项通过，运维说明文件不存在的断言按预期失败；补充本文档与运维说明后进入 GREEN。
- 分包边界 RED：迁移前先更新路径和包结构断言，得到预期 15 项失败；实现后 7 个目标测试文件、93 项测试通过。
- 首次完整测试发现 2 个阶段间测试契约问题：辅助分包被错误断言为只能包含一个页面，云初始化测试缺少网络恢复回调 mock。修正后对应 7 项定向测试及完整测试通过。
- 安全门禁 GREEN：`account-sync-security.test.ts` 3 项测试通过。
- 最终包体实测：主包 1,563,190 字节、138 个文件，低于 1.5 MiB；`packages/auxiliary` 69,452 字节、36 个文件，低于 2 MiB。
- 主流程最终 `npm run verify` 通过：TypeScript、ESLint、Stylelint、Prettier、包体检查全部通过；Vitest 48 个测试文件、356 项测试通过。
- 独立 tester 复验通过：13 个账号/同步测试文件、79 项测试通过；随后 `npm run verify` 再次通过，完整结果仍为 48 个测试文件、356 项测试。
- reviewer 首轮只读审查发现 2 项 CloudBase 真实读写契约问题、2 项恢复/清除状态问题、索引唯一性文档偏差和响应体无上限；均已通过上述独立修复提交及运维文档修订处理。
- 修复后的独立 tester 复验通过：10 个定向测试文件、73 项测试通过；`npm run verify` 再次通过，完整结果为 48 个测试文件、365 项测试。
- reviewer 复审发现 NotFound 配置层级和清除期间资料同步竞态仍需调整，随后由 `7a1e900`、`5c3b23d` 修复。
- reviewer 最终复审通过：此前 6 项均已关闭，未发现剩余或新增 P0–P2；其定向 10 个测试文件、70 项测试及 `npm run verify` 均通过。
- 主智能体最终 `npm run verify` 通过：TypeScript、ESLint、Stylelint、Prettier、包体检查全部通过；Vitest 49 个测试文件、367 项测试通过。

## 云端状态

本地阶段没有创建集合、修改数据库权限、创建索引、部署云函数、打开计费或配置密钥。下一步必须先向维护者展示精确变更清单并暂停，取得明确确认后才能操作云端。
