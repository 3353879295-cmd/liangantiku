# 2026-09-08 上传说明与验收入口

> 历史记录：本页描述 2026-09-08 至 09-09 的排障状态，以下“仍不能上传”及待部署包均不是当前结论。2026-09-15 已完成真实支付闭环，当前版本、部署包与逐项验收结果以 [最终支付验收报告](2026-09-15-person-payment-acceptance.md) 为准。

准确开发目录：`D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification\miniapp`。不得切换根目录副本。最新真实验收状态以 [真实写入恢复记录](2026-09-08-real-write-recovery.md) 为准。

## 2026-09-09 上传前最新判断

**本地客户端上传材料已准备，但现在仍不能上传。** 这是完整上线验收结论，不是对 Active、HTTP 200、terminated、部署成功或新增日志的替代解释。未授权提交审核或正式发布。

当前 membership 已部署包为 20:17:05 控制台直接普通保存的 `membership-platform-error-standard.zip`，SHA-256 `a4ee8185b4c251d1db74aa9392f3deca8a252cff6934e0b5d0276e78ae6677db`；它也是回滚包。第二旧单一次正常 `getOrder` 的 RequestID 为 `4b33a6ba-1130-424d-8b1e-f4bf924084ce`，2762ms 后仍是 `MEMBERSHIP_UNAVAILABLE`，未新建/关闭/修改订单、未付款，平台 errcode 未取得。

正式待部署包是 `tmp/final-release-20260908/membership-platform-error-response-v2-standard.zip`：SHA-256 `235d5cfb4a041205e9d077dd974c3e8a5434dd3f02d9a92e14d8f500682a1501`，6,996,402 bytes、5,824 files、100644，CRC、路径和源码一致。它只对白名单 `ORDER_PLATFORM_ERROR` 加有界安全整数 `platformCode`，继续返回原 `MEMBERSHIP_UNAVAILABLE`；相对回滚包仅 `diagnostics.js`、`handler.js`。非 v2 草稿 `membership-platform-error-response-standard.zip`（SHA-256 `4d68ebf275f054e8590a440d616e49145f44510060ae0ec3f6717ed651f4f40d`）权限元数据不符合本轮标准，未部署且禁止上传。仅可用控制台直接选择标准 ZIP 后普通保存，禁止 CLI、文件夹、重打包和安装依赖。

控制台恢复后，唯一下一步是部署正式 v2 后以同一第二旧单参数正常查询一次；必须拿到 `diagnostic.platformCode` 或合法订单终态，异常即回滚 `a4ee...` 包。在此之前不能继续真实查单或支付。上传根 sourceMaps=0、无 private tmp；依赖自带 53 个 map 必须与上传根情况分开如实记录。

仍需逐项完成：v2 云部署和第二旧单平台码/终态；两条旧单安全处理后由本人在既有 ¥56 总上限内完成 ¥28 首购及 ¥28 续费、通知/权益/幂等/断网恢复闭环；iPhone 使用新预览的恢复、断网、导航和显示；旧基础库实际切换验收；云 SDK 告警风险处置。完整 DevTools 窗口截图仍无法由原生 App 接口获取，不能以模拟器局部图冒充；需维护者提供完整截图，或手动在“详情 → 本地设置 → 调试基础库”切换后由本任务读取 SDKVersion。

## 当前结论

**现在还不能按本任务的上线标准交付上传。** 客户端已通过官方完整预览编译与全部本地检查，包体和上传配置满足要求。accountSync 真实写入、新随机授权、会员 HTTP 412 查单故障均已验收；剩余阻断集中于两条旧待支付订单的安全终态、真实付费闭环、手机/旧基础库操作覆盖，以及未解决的云 SDK 依赖风险。

原会员单已真实查单三次成功，但平台仍返回未完成状态（0 或 1）；当前不能重用原交易号，也没有查到适用的未付款关单接口或保证的自动关闭时间。草稿和具体平台问题已准备，见 [平台订单问题](2026-09-08-platform-order-support-draft.md)。不能删订单或指针来通过验收。

## 已验证的版本变化

- accountSync 在原事务和修订校验内保存完整进度快照，修复 SDK 将对象更新展开到 null 父节点导致的真实写入错误。原四条待同步记录经正常确认机制处理，学习记录、答案和原偏好已独立核对，最终修订 profile20/progress3、pending0，重进仍一致。
- 修订冲突持久暂停并保留命令与缓存；偏好响应和发送值不一致时同样暂停，防止错误确认。
- 清学习数据和注销相关三个入口补齐二次确认；未执行真实清除或注销。
- 会员 HTTPS 请求使用正确 UTF-8 正文长度，修复真实 HTTP 412；18:51:46 已控制台部署，原会员页三个 getOrder 均业务成功，云日志确认 completed/status-read。尚不能把整个支付恢复流程写成已完成。
- 用户手机 18:34:50 新随机授权真实成功，日志摘要与云 grant 相等，当天 3 个授权与已用 3 次一致。手机同一会话重进未扣次仍需本人操作；历史首次失败原因没有被反推为已解决。

当前最终本地：70 文件、656 项单 worker 测试；类型、Lint、格式、正式包预算、accountSync bundle 均通过。主包源码 1,557,109 / 1,572,864 字节，余 15,755；辅助包源码 106,739 字节。2026-09-09 重新生成的官方预览仍是客户端 70/653 基线，压缩主包 1,425,610 字节、辅助包 91,721 字节、总计 1,517,331 字节；新增 3 项只覆盖云函数诊断，不改变该客户端预览。源码预算和预览压缩体积是两种不同口径。

## 完成阻断后的上传步骤

1. 在微信开发者工具确认准确目录及 AppID `wx84ecacec08ca162c`，沿用现有账号、云环境、上传设置。`miniprogramRoot` 必须为 `miniprogram/`，不把 worktree 根、tmp、备份或云函数 ZIP 作为小程序上传目录。
2. 如重新安装依赖或执行官方“构建 npm”，接着在准确 miniapp 运行 `npm run prepare:runtime-deps`。官方 npm 构建对 fflate 的 `.cjs.js` 入口有已知告警；仓库脚本从锁定依赖的官方 UMD 恢复有效运行产物，不能忽略丢失的导入文件。
3. 有任何源码/依赖变化时重新执行 `npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run check:package`、`npm run check:account-sync`、`npm test -- --maxWorkers=1`。不得提高预算或放宽超时。编译后检查实际页面及 Console。
4. 使用已生成的预览码进行真机验收。当前二维码为 `tmp/final-release-20260908/final-client-v3-preview-qrcode.jpg`；生成记录见 `final-client-v3-preview-safe-evidence.json`。生成预览码不等于已经上传体验版或通过真机验证。
5. 阻断关闭后填写维护者确定的版本号，当前真实版本说明可使用：“修复账号进度同步，保留冲突记录并校验偏好确认；完善清除与注销确认；修复会员订单查询的网络传输故障。”不要写成支付开通、续费或全部订单恢复已验收。
6. 上传仅供后续体验核验；本任务未执行小程序 upload，未授权提交审核或正式发布。云函数部署另走控制台标准 ZIP 普通“保存”，不使用小程序上传功能或 Windows CLI 代替。

## 剩余必要动作

| 项目                       | 已采取措施                                                                                                 | 仅剩动作与成功标准                                                                                                                                                                                        |
| -------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 两条历史待支付订单         | 原单权威查询已成功；全部订单和本地待支付记录保留；旧单无云指针仍由下单事务的 legacy 分支保护；独立审查通过 | 平台确认两条旧单的准确状态并提供适用的关闭/安全重购协议。只在可核验的终态下由正常客户端恢复；另一条订单也不能漏过。工单草稿已就绪，未发送。                                                               |
| 首购、通知、断网恢复、续费 | 金额/身份/签名/幂等/通知的本地回归通过；原通知路由和自然定时任务已核对                                     | 旧订单安全处理后，由维护者本人在现有 ¥56 总上限内完成一次 ¥28 首购和一次 ¥28 续费；本任务逐笔核对真实通知、订单、权益和到期时间，并验收付款后断网重进没有重复发权益。当前不应点击新购。                   |
| 手机随机练习恢复           | 手机新授权、云日志、grant、当日 usage 已一致                                                               | 在刚才同一手机、同一练习用页面正常返回首页再点“继续”，确认题序/答案保留且次数仍为 3；断网后重连，再看“我的”是否同步完成。不要新开一次练习或清数据。手机本地状态需要本人提供，不能借用电脑缓存代替。       |
| 真机导航和显示             | 模拟器自动返回、Tab 导航已有真实证据                                                                       | 在维护者现有 iPhone 上用本轮新预览核对账号恢复后正确返回、首页/实操/我的切换、日夜模式、大字号和安全区；记录机型、系统、微信版本及具体失败页面。已经实际操作的项直接记录，不重复操作收费入口。            |
| 旧基础库                   | 配置切换试验未使运行时进入旧版本，配置已逐字节恢复；当前实际 SDKVersion 为 3.17.0                          | 开发者工具“详情→本地设置→调试基础库”实际选择代表旧版本 3.0.0；本任务读回 SDKVersion 后验收启动、账号返回、Tab 与会员查询，最后恢复 3.17.0。若下拉不存在该版本，提供实际可选旧版本；不把改配置当运行通过。 |
| 云 SDK 告警                | 前端生产 0；accountSync/membership 各 6 项（5 高、1 中，含聚合项）；核对官方依赖链，无已验证自然升级方案   | 官方兼容依赖修复并验证，或上线负责人对 [剩余风险](2026-09-08-final-release-work.md#依赖风险) 作明确处置。没有 force fix、降级或未经验证 overrides，告警仍未解决。                                         |

## 已部署包及回滚

- accountSync：`tmp/final-release-20260908/accountSync-progress-replacement-standard.zip`，SHA-256 `1ec3a122f2bb6585df4fa0fb54e142f6838a471476f568363cc46f91a95f76c5`；回滚为 `tmp/prelaunch-fix-2026-09-08/console-recovery-20260908/accountSync-diagnostics-v2-standard.zip`。
- membership 当前线上：`tmp/final-release-20260908/membership-platform-error-standard.zip`，SHA-256 `a4ee8185b4c251d1db74aa9392f3deca8a252cff6934e0b5d0276e78ae6677db`。它是待部署 `membership-platform-error-response-v2-standard.zip` 的直接回滚包；v2 SHA-256 为 `235d5cfb4a041205e9d077dd974c3e8a5434dd3f02d9a92e14d8f500682a1501`，当前**尚未部署**。更早的 HTTP 传输包和诊断包只作为历史恢复材料保留，不能用“曾部署并验收”替代本轮 v2 部署和第二旧单真实复查。
- 小程序上传根仅为准确 `miniapp/miniprogram/`；私有备份、原始日志、云函数 ZIP 与证据都在外部 `tmp`，不得提交或打入小程序上传包。代码回滚不会撤销已经成功同步的数据或交易。
