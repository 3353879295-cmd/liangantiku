# 保管员刷题：新窗口交接（2026-09-08）

## 2026-09-09 最新接续状态（优先于下文历史）

- 20:17:05 已在控制台直接以普通部署保存 `tmp/final-release-20260908/membership-platform-error-standard.zip`，SHA-256 `a4ee8185b4c251d1db74aa9392f3deca8a252cff6934e0b5d0276e78ae6677db`。该包是当前已部署版本，也是待部署 v2 的直接回滚包；云函数只可控制台直接选择已核验标准 ZIP 普通保存，禁止 CLI、文件夹上传、解压后重打包或安装依赖。
- 2026-09-09 接手时没有遗留 wechatide、部署或查单进程。项目最初关闭，后来仅以准确 `miniapp` 的 fullMode 打开；运行时为首页、iPhone 12/13 Pro、SDK 3.17.0，未打开根目录副本。
- 新部署后，第二条历史待支付订单只执行了一次正常 `getOrder`：RequestID `4b33a6ba-1130-424d-8b1e-f4bf924084ce`，耗时 2762ms，业务结果仍为 `MEMBERSHIP_UNAVAILABLE`。这不是 terminated、HTTP 200 或 Active 意义上的成功；未新建、关闭或修改订单，也未付款。腾讯云旧/新标签的 AX、DOM、CDP 均超时，准确平台 errcode 仍未取得。
- 为脱离日志页阻断，当前本地增加严格白名单响应：仅内部 `ORDER_PLATFORM_ERROR` 且 `platformCode` 为有界安全整数时，公开 `diagnostic`；原错误码仍为 `MEMBERSHIP_UNAVAILABLE`，其余错误形状不变。安全 reviewer 无未解决 P1/P2。单 worker 全量为 70 文件、656 项；类型、Lint、格式、包预算、accountSync bundle 均通过。主包 1,557,109 / 1,572,864 字节，余 15,755；辅助包 106,739 字节。
- 正式待部署包为 `tmp/final-release-20260908/membership-platform-error-response-v2-standard.zip`，SHA-256 `235d5cfb4a041205e9d077dd974c3e8a5434dd3f02d9a92e14d8f500682a1501`，6,996,402 bytes、5,824 files、100644；CRC、路径、源码一致。相对当前回滚包仅 `diagnostics.js`、`handler.js` 不同。非 v2 草稿 `membership-platform-error-response-standard.zip`（SHA-256 `4d68ebf275f054e8590a440d616e49145f44510060ae0ec3f6717ed651f4f40d`）权限元数据不符合本轮标准，未部署且禁止上传。依赖自带 53 个 map 必须如实区分；小程序上传根 sourceMaps=0，且无 private tmp。
- v2 仍未部署，不能继续真实查单或支付。控制台恢复后唯一下一步：函数代码直接选正式 v2 ZIP、普通保存（不安装依赖），再使用相同第二旧单参数正常查询一次；必须取得 `diagnostic.platformCode` 或合法订单终态。异常时回滚上述 `a4ee...` 包。
- accountSync 已真实恢复至 profile20/progress3、outbox0，学习数据、偏好和答案保留。随机入口修复及最新客户端预览证据仍以 70/653 为基线；2026-09-09 重新生成的二维码为 `tmp/final-release-20260908/final-client-v3-preview-qrcode.jpg`，压缩主包 1,425,610，仍有效。手机已有 grant、当天 usage=3；不得用电脑旧 pending 消耗新额度。
- **现在不能上传。** 本地客户端上传材料已准备，但完整上线验收仍受以下精确阻断：v2 云部署与第二旧单平台码/终态；两旧单安全处理后由本人完成 ¥28 首购和 ¥28 续费闭环；iPhone 新预览恢复、断网、导航和显示；旧基础库实际切换验收；云 SDK 告警风险处置。未授权提交审核或正式发布。
- 完整开发者工具窗口截图仍因原生 App 接口不可用而无法取得，不能用模拟器局部图冒充。需维护者提供完整窗口截图，或手动在“详情 → 本地设置 → 调试基础库”切换后，由本任务读取 SDKVersion 验收。

## 最重要的当前状态

用户要求继续修复所有上线问题，已授权 membership 部署及故障回滚，并在本轮“确认”中批准精确 accountSync v2 诊断包部署和只读验收；没有授权真实扣款或本轮队列重放。**2026-09-08 12:16 后接续更新：membership 已经通过控制台标准 ZIP 恢复且新版真实只读验收通过；accountSync v2 也已由控制台部署，真实 bootstrap 和诊断日志通过。账号写入故障及其他上线阻断尚未全部解决，暂不上传体验版。**

### 接续完成事项与当前下一步

- 用户重新扫码登录后，主智能体通过云控制台直接上传标准恢复 ZIP，普通“保存”；旧版真实 `getStatus` 与原 session 验证均成功。再直接上传原授权 `membership-local-validated.zip`，11:11:59 部署完成，新版两项真实调用再次成功。两版四次均维持 `freeUsed=2/freeRemaining=1`，支付配置可用。没有用 CLI 部署，没有改配置、触发器、密钥或数据。
- 新版验证 RequestID：状态 `0a3e0017-47a2-4f1a-b228-9a3cc898dbc2`，授权 `2d0f0386-9bb5-4392-8ac5-e074524b3fb2`。实际云日志已出现新版随机诊断 revision。11:30 自然定时调用 `63aacdd2-310b-4b66-9f14-371e660d2bd2` 来源 `TRIGGER_TIMER`，业务 `ok=true/processed=2`；未主动触发补单。完整证据见 [部署事故记录的接续部分](2026-09-08-membership-deploy-incident.md)。
- accountSync 最近一小时的真实云日志已证实 `saveActiveSession` 在服务端返回 `ACCOUNT_SYNC_UNAVAILABLE`（10:47:13，RequestID `c1bc9d0f-4f9c-44cb-85f0-0ffd5063a3a0`）。底层事务错误未记录，不能确定根因。云1历史日志查询触发升级套餐提示，未购买。用户确认后，精确 v2 标准 ZIP 已于 12:14:50 由控制台普通部署到 `cloud1-d2gglad830c91db10 / accountSync`，保留 Nodejs20.19、index.main、256MB、20秒及其余配置。真实 bootstrap 请求 `97f65fb4-9c3f-4815-8d39-ed087e0d55d2` 返回 ok、修订19/0；实际云日志记录新诊断 revision 和事务提交成功。详见 [诊断部署和验收记录](2026-09-08-account-sync-diagnostics.md)。**诊断部署已完成，不要再次申请相同授权；写入根因和恢复仍未验收。**
- 新发现客户端 `REVISION_CONFLICT` 路径会清空 outbox 并覆盖本地缓存的 P1 风险，已在本地修复为持久化冲突暂停：保留原命令及缓存、重启和后台不重放、blocked时仅追加新命令、不合并或rebase，公共takeNext也直接拒绝。独立tester最终单worker全量 **68文件、619项通过**；独立reviewer提出的出队P2已修复并复核。该前端尚未在真实模拟器运行或上传小程序版本。修改前已只读备份本地账号缓存、4 条 pending 及清理标记，修订仍19/0，clearPending=false。由于 `compileHotReLoad=true`，已用官方 `close_project_window` 关闭准确 worktree 项目窗口，防止前端编辑触发后台发送。**不要未经明确队列重放授权重新打开该项目或启动模拟器。** 私有备份 `tmp/prelaunch-fix-2026-09-08/console-recovery-20260908/account-local-preservation-private.json` 不能展示业务内容或提交到版本库。
- 当前客户端冲突保护的静态检查通过；2worker全量曾有1项题库完整性超时，最终单worker为68文件、619项通过，主包 **1,572,640 / 1,572,864，余224字节**。再早的accountSync诊断阶段为2worker618通过、默认并行15项超时及清理异常；所有失败日志仍保留，不能把默认 `npm run verify` 写成已通过。613项与24字节余量均是更早基线。详见诊断记录的客户端冲突保护部分。
- membership 历史日志正常分页已读到昨晚异常窗口：23:54:47 的 `f93f42ea-a788-406d-9f11-29c8613bdccc` 实际返回 `MEMBERSHIP_UNAVAILABLE`。旧日志未记录 action/session 或底层错误，仍不能确定与原 session 的唯一关联或根因。勿为了取证新增扣次请求。
- 下文恢复包“尚未上传”、浏览器不可用和服务未恢复均是先前历史状态，被本节更新；不要再次执行恢复部署，也不要等待旧 CLI 弹窗。保留学习数据、额度和 4 条待同步记录，不刷新/重编译模拟器触发重放。

本轮误用了 Windows 微信 CLI 部署：先把已验证的标准 ZIP 解压，再交给 CLI 重新打包。新版本部署后 getStatus 连续失败；用相同 CLI 回滚旧代码也失败。症状与项目已证实的 Windows 归档路径故障完全一致，当前优先判断为该问题复发；本次尚未直接读取云端目录布局，应以标准 ZIP 直接上传后的真实运行结果进一步确认。已经向用户承认漏看部署限制，不要重复同样的操作。

**不要再用 `cloud_fn_deploy`、传统微信 CLI deploy/inc-deploy 或“上传文件夹”部署 membership。必须在云控制台直接上传标准 ZIP，不能解压后让 CLI/控制台重打包。** CLI 的只读查询、下载、模拟器验证仍可使用。

## 1. 项目与工作区保护

- worktree 根：`D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification`
- 实际小程序：`D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification\miniapp`
- 不要切换 `D:\文档\保管员刷题小程序\miniapp`。多次 `project_list` 已确认开发者工具当前打开的是正确 worktree。
- AppID：`wx84ecacec08ca162c`；当前页面品牌名“粮安题库”。
- 工作区有大量原有未提交/未跟踪文件，不要回滚、覆盖、清理或把已有工作算作本轮成果。
- 最初修改前完整基线：`tmp/prelaunch-fix-2026-09-08/baseline/`，含 git-status、diff、386 个工作文件副本及 SHA。HEAD 为 `266b955593e4c3e1f7a27223dc9d37313e36f9f9`。所有路径无特别说明均相对 worktree 根。
- 未发现实际磁盘 AGENTS.md，执行用户提供的规则：仅使用配置好的 scout、implementer、tester、reviewer；有明确独立子任务才并行；代码稳定后独立 tester，涉及公共契约/安全用 reviewer。最多同时 4 个子智能体，写入所有权不重叠。
- 价格仍为 28 元 / 6 个日历月，非会员每天 3 次随机练习。题库、学习数据、额度、待同步队列禁止清空重置。

## 2. 接手后优先阅读

1. **`docs/membership-backend.md` 第 42–64 行**：明确禁止 Windows CLI 上传，规定控制台直接上传标准 ZIP。
2. **`docs/qa/2026-09-05-membership-validation.md` 的“云端启动故障定位与打包修复”**：当时完全相同的 `-504002 / 0 code exit unexpected`，云端 /var/user 的数千顶层条目是带反斜杠的文件名，实际没有 lib/node_modules 目录；标准 ZIP 直接上传后恢复。下载回 Windows 再比对文件 SHA 会掩盖目录结构错误，不能据此认定云端文件布局正确。
3. `docs/qa/2026-09-08-membership-deploy-incident.md`：本轮新版本故障与回滚请求记录。注意其文末“回滚 pending”已被本交接更新，回滚后来执行成功但服务仍失败。
4. `docs/qa/2026-09-08-deployment-validation.md`：导航 onRouteDone 修复、账号恢复失败的只读取证、613 项验证。
5. `docs/qa/2026-09-08-prelaunch-fixes.md`：本轮支付/随机诊断/依赖/部署方案的完整证据。
6. `docs/qa/2026-09-07-prelaunch-polish.md`：上一轮历史报告，不覆盖。

## 3. 历史部署事故与恢复包（已恢复，勿重做下述部署）

目标环境：`membership-staging-d7c032b6e3273`，函数 `membership`。develop/trial/release 都指向这个环境，名称 staging 不代表与客户端隔离。

只读状态仍显示 Active、Nodejs20.19、超时 60 秒；过去已验证配置是 index.main、256 MB、原十分钟补偿触发器。当前工具只返回 runtime/timeout/status，不能声称其余配置已经逐项复核。不得修改密钥、数据库规则、触发器或全局云环境。

### 部署/回滚任务均已成功结束，不再等待旧弹窗

- 新版任务：`confirmation_cloud_fn_deploy_c6a0c086-08aa-4903-8072-9247c26c5011`，07:32:38 成功，5,824 文件。
- 回滚任务：`confirmation_cloud_fn_deploy_6edf51b7-5e16-4141-b302-234b1df4748c`，后续已查询为 success，5,821 文件。
- 回滚后真实 getStatus 仍失败：北京时间 07:44:51 发起，5,828ms 后 `-504002 / 0 code exit unexpected`，callId `1788824691512-0.7555969272276741`。
- 新版两次失败 callId：`1788824084100-0.6875589387176225`、`1788824145382-0.04502487568121216`。
- 本轮没有真实下单/付款/发权益，没有主动调用补偿/通知/查单协调去制造云写入。不要把部署成功等同于运行验收成功。

### 已完成恢复所用的标准回滚 ZIP

完整路径：

`D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification\tmp\prelaunch-fix-2026-09-08\authorized-deploy\membership-rollback-standard.zip`

- SHA-256：`54ba3814a74803789a1300a9edc95734bbfa60f25cc5c75f4ffca5d71e0b95b2`
- 5,821 文件，6,991,809 字节。
- 从部署前真实线上备份生成，ZIP 条目是 POSIX 正斜杠路径，Unix 文件模式 100644；每项内容与旧版本逐字节一致，包含 lib/cloud-store.js 与 node_modules/wx-server-sdk/package.json，已 testzip 通过。
- 证据：`tmp/prelaunch-fix-2026-09-08/authorized-deploy/standard-rollback-evidence.json`。
- **本轮接续已直接上传此标准回滚 ZIP 并恢复运行，随后已部署新版。** 更早的失败回滚是 CLI 重打包目录，不是上传此 ZIP。

控制台步骤：目标环境 → 云函数 → membership → 函数代码 → 上传代码 → 点击选择 → 选择压缩包 → 选上面 ZIP → 普通“部署”。不要选“部署并安装依赖”，不要部署在线编辑器残留模板，不要上传文件夹。

旧代码标准 ZIP 恢复和下述新版标准 ZIP 部署均已完成真实只读验收，见本文件顶部；这些步骤只保留事故追溯，不能重复执行。

### 已测试的新版本标准 ZIP

`D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification\tmp\prelaunch-fix-2026-09-08\membership-local-validated.zip`

- SHA-256：`c2144452f827b3e2de835bc89352f2a78f147ed70b30fee9e551325ff3a6c5fb`
- 5,824 文件；已逐文件比对打包源码。仍含已披露的 SDK 传递依赖告警。
- 只允许直接上传这个 ZIP，不能解压后再次 CLI 部署。
- 原始备份：`tmp/prelaunch-fix-2026-09-08/before-authorized-deploy/`。
- 回滚工作目录：`tmp/prelaunch-fix-2026-09-08/authorized-deploy/rollback/membership/`。
- 新版/旧版下载比较证据分别为 `membership-zip-deploy-evidence.json`、`rollback-file-evidence.json`。它们证明内容一致，**不证明 Linux 上的归档路径正确**。

## 4. 工具与阻碍

微信官方 CLI skill：

`C:\Program Files (x86)\Tencent\微信web开发者工具\resources\app.asar.unpacked\wechatide-skill\SKILL.md`

已授权客户端 `codex-release-audit`，不要创建新的 client 引起额外授权。门禁版本 0.3.5，已登录；常用只读命令：

```powershell
wechatide -c codex-release-audit project_list
wechatide -c codex-release-audit cloud_fn_info --appid wx84ecacec08ca162c --env membership-staging-d7c032b6e3273 --names membership
```

云写工具会另弹微信界面确认，pending 不等于执行成功；已有两个任务都结束成功，不要重发。**后续恢复必须使用控制台 ZIP 上传，不能再次调用这些 CLI 部署工具。**

历史界面阻碍（本轮用户扫码后控制台已可用）：

- CUA inventory 能看到 Edge（id 2，profile cheng）里的公众平台“ 小程序 ”标签页（id 35665359），但 getTab 多次超时。
- 最新尝试 `cua.createBrowserTab('edge','https://console.cloud.tencent.com/tcb',...)` 15 秒超时；不能认定新标签已打开或已登录。接手先 inventory，不要盲目重复动作。
- 原生 computer-use 的截图曾因 `SetIsBorderRequired ...0x80004002` 失败，UIA 信息很少。不要虚构看到了控制台。
- 如果仍无法控制云控制台，请让用户只完成上面的“直接上传标准 ZIP”步骤；其余本地核对、只读验收由你继续。用户已授权同环境恢复/部署，不必重新询问概念性许可。
- 官方 CLI 没有云日志读取命令；浏览器/控制台可用时，应读取实际云日志，勿提取浏览器凭据绕过登录。

模拟器只读 getStatus 的参数文件：`tmp/release-polish-status-args.json`；原授权 validate 参数文件：`tmp/release-polish-validate-args.json`。用 `--args-file` 传 JSON，避免 Windows CMD 吞掉 JS 引号。

```powershell
wechatide -c codex-release-audit automation_evaluate --project 'D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification\miniapp' --fn-source 'async options=>{const started=Date.now();const r=await wx.cloud.callFunction(options);return {elapsedMs:Date.now()-started,requestID:r.requestID,result:r.result};}' --args-file 'D:\文档\保管员刷题小程序\.worktrees\warehouse-question-classification\tmp\release-polish-status-args.json'
```

恢复后核对真实返回 ok、paymentAvailable=true、freeUsed=2/freeRemaining=1（同一天未有其他使用时）；原 session validate 成功且不扣次。若日期变化，按服务端日期解释额度，不能强行改回旧数值。

## 5. 本地代码已经完成的修复

- 虚拟支付：官方 coupon_fee 可缺省兼容；权威关闭后在事务内释放匹配待支付指针，旧订单查询不能清新指针；取消提示不误报旧会员失效；待支付订单可复制，剪贴板拒绝被消费。
- 不删除本地订单、不伪造服务端终态、不任意超时关单、不套用 JSAPI 关单、不重复拉起旧 outTradeNo。
- 随机授权：新增会员诊断模块（固定 revision `membership-random-diagnostics-20260908-v1`），客户端分阶段/耗时/安全错误码/RequestID；保持原子性、幂等、空题不扣次、12 秒单次截止，无新增重试。历史首次失败根因未证实。
- 导航：账号页单次离开锁、旧回调/卸载隔离；自动返回等待官方 onRouteDone，不再在 onReady 提前导航。首页连续三轮未包装复现全部清栈，从实操入页也正常回首页。官方依据：https://developers.weixin.qq.com/miniprogram/dev/reference/api/Page.html#onRouteDone 。真机和旧基础库未验证。
- 既有 TabBar 最后点击目标/高亮/异常释放、账号同步后答题按钮修复保持回归；它们原已完成，不算本轮新增。

诊断与客户端冲突保护变更前的历史基线 `npm run verify`：**68 文件、613 项测试全部通过**；TypeScript、Lint、格式、包预算、accountSync bundle 均通过。该时点主包 **1,572,840 / 1,572,864 字节，余 24**；auxiliary 105,621 字节。独立 tester/reviewer 已完成。日志：`tmp/prelaunch-fix-2026-09-08/authorized-deploy/local-verify-final.log`。后续变更的验证以顶部及诊断记录为准，不能将613项结果当作当前代码全量通过。

Python 257 通过、1 跳过，无后续 Python/数据变化，不必重复；命令是根目录 `.\.venv\Scripts\python.exe -m pytest tests`，不要扫描 tmp 基线副本。

## 6. 仍未解决的其他上线阻断

### 长期待支付

当前是 `wx.requestVirtualPayment` 个人道具直购，28 元/6 个日历月。取消 -2 不能证明服务端订单关闭；查单 1/NOTPAY 继续 PENDING，未知结果保留恢复。权威查单 6/关闭后才允许创建新交易号。官方公开现金直购接口没有可直接套用的主动关单能力，长期 NOTPAY 仍不能保证立即重买。报告已给订单号反馈/平台核查流程，不能说该问题完全解决。

### 账号同步失败（新发现，数据要保留）

显式 mode=login 页面 phase=error，提示账号记录暂不可恢复。本地原 outbox 有4条：saveActiveSession(expected0)、saveActiveSession(1)、recordPractice(2)、updatePreferences(19)。自动启动会重放，当前失败。**不要清队列，也不要为了复现不断编译导致重复云写尝试。**

当前账号缓存 profileRevision19/progressRevision0；只读定点查数据库证实账号 active/idle/rev19，progress rev0/answered0。原练习还在本地待同步，不能当作无数据删除。

accountSync 在 `cloud1-d2gglad830c91db10`。诊断部署前的旧入口 SHA 为 `52c5946bb5ef2ed8096b6515dce60f13060f72215ba26141fe84a8e63444b541`；旧版只读 bootstrap 请求 `dcc97eb2-f1b6-45f2-b713-ebdd2f197a32` 已证明事务读取和身份可用。当前已经部署 v2 标准诊断 ZIP；新入口本地 SHA 为 `032fd01720aacdfe795b7af92bd56ffccc442cb3157e7020c4e4473d44ceded8`，真实执行新日志且只读通过，但未独立下载云端源码逐字节比较。

真实待同步事件本地验证及实际 handler 模拟成功；线上写后修订仍0。剩余范围是 saveProgress/事务写提交或返回边界；事务内部错误会向外抛，未发现吞错破坏回滚。原网络 HTTP200 不含业务body，不能据此判断成功。新诊断可用于下一次真实写入取证；下一步先完成客户端冲突保护的独立测试和审查，再就准确的队列重放范围请求云写授权。不能脱离 outbox 确认机制手工调用写请求或自行删队列。

### 首次随机授权/安全/真机

- 原 session `random-1788796485987-hvtamvcpezc-wy7w2tafa`，原首次失败时间09-07 23:54:45；后续恢复成功未重复扣次。需要当时membership云日志或新诊断上线后的对应证据，不能归因 Promise.all 或增加重试蒙混。
- 前端生产依赖审计0；membership/accountSync各6项（5高1中），wx-server-sdk4.0.2的axios/lodash传递依赖。未找到已验证兼容修复，禁止force fix、降级、未经验证overrides。
- 真实会员下单、支付、通知、查单、开通、取消重买、扣款后断网恢复、续费均没有完整真实验收。Mock通过不等于真实支付通过。真实扣款需要另行明确授权，当前部署授权不包含付款。

## 7. 新窗口执行顺序

1. 先读本文件顶部当前状态及 accountSync 诊断记录；membership 恢复/新版部署和 accountSync 诊断部署均已完成，不重做。
2. 客户端冲突保护实现、独立tester和reviewer已完成，准确证据见诊断记录，不重复检查替代线上修复。当前仍未有真实队列重放授权。
3. 下次授权须明确：正常同步入口会逐条尝试全部4条学习/偏好命令，成功后由outbox正常确认；启动恢复成功还可能清理未绑定的pending头像云文件，当前pending头像数未确认。若获准正常恢复则继续取真实日志，不能擅自打开已关闭的项目；不包含付款或新增随机练习。
4. 获得该授权后按客户端正常确认流程取真实诊断，失败保留数据；不能凭 bootstrap 或平台200宣称写入恢复。
5. 继续处理支付、SDK依赖和真机剩余阻断，真实付款需另行明确授权。代码变更后才重跑相应测试及必要全量，记录真实/模拟/未验证状态。

用户很在意实际解决问题和避免反复确认。已有具体部署/恢复授权继续有效；不要让用户重新复述上下文，也不要把尚未通过的线上链路标成完成。
