# 待支付订单无法重新购买：平台问题草稿

此为本地草稿，尚未发送。提交平台支持前，由维护者在小程序会员页复制原订单号，填入工单的私有订单字段。第二条同账号旧订单也需平台定点核查；不要在公开反馈中贴出完整订单、用户身份、token、签名或密钥。

## 建议正文

我们使用个人主体小程序虚拟支付，AppID 为 `wx84ecacec08ca162c`，mode 为 `short_series_goods`，env 为 `0`，会员商品金额 2800 分。用户曾打开支付后留下未完成订单，当前该账号本地订单库有两条旧 PENDING。为避免重复扣款，应用保留订单，未重用商户单号、未删除待支付记录或另开新单。

2026-09-08 18:52 北京时间，原订单经 `/xpay/query_order` 正常服务端查询成功，响应通过订单身份、金额和环境校验。业务映射为未付款/未完成（适配器仅将平台 0 或 1 映射为该状态，没有在安全日志中保留更细数字）。云请求 ID 为 `c919b19e-8084-4f3e-8477-7405526cad5a`；另两次 `886c7d39-dadd-42b5-8bea-c881667b35e8`、`acfdac29-65a1-447f-b504-4b20f131688e` 结果相同。另一条旧单需要平台一并定点确认，不能仅依据我们数据库状态判断。

请确认以下问题，并给出适用于本主体与模式的接口文档或明确操作条件：

1. 这两条订单各自的权威状态是什么，是否还有可能继续支付或产生延迟成功通知？
2. 用户取消但订单长期处于 0/1 时，是否支持关闭未付款订单？如支持，请提供适用接口、权限、签名及成功后查单状态。
3. 是否存在官方保证的自动关闭时限？若没有，平台能否将确认未付款的旧订单处理到可验证的状态 6？
4. 若允许重新购买，必须满足哪些可校验条件？官方“交易号只能使用一次”的约束下，有无允许重拉原单的特定例外？

我们需要以平台终态或正式确认的协议实现安全恢复。不能依赖删除客户端记录、任意等待时长或普通 JSAPI 关单接口。

## 收到回复后的验收

保留旧订单及审计记录；只有平台权威终态/正式协议适用且完成实现验证后，才由原客户端正常恢复路径处理。现有代码在查到关闭状态时会事务性关闭旧单并只释放匹配指针；有另一条旧单时仍会拦截新购。不得由维护者手改数据库为关闭来代替平台结果。

所有旧待支付订单安全处理后，由维护者本人在已授权的总上限 ¥56 内依次验证一笔 ¥28 首购与一笔 ¥28 续费；每笔分别核对真实通知、查单、权益、幂等，并覆盖付款后断网重进。该授权不包含更多订单、退款、购买云套餐或正式发布。
