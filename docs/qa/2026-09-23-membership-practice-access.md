# 免费练习权限与会员弹窗修复

## 行为

- 免费用户每天只能开始 3 次随机练习，仍沿用云端次数与试卷授权机制。
- 章节、顺序、错题本、收藏、模拟考试和实操相关练习均要求会员，包括新建、重练、继续旧练习和直接进入练习页面。
- 首页拦截入口，练习运行时再次校验云端 `fullPractice`；网络异常、畸形或矛盾的权限响应不会放行。
- 本次随机练习提交后的报告、答题卡和本次错题解析继续免费查看，不重新扣除次数。
- 权限请求等待期间离开页面或切换账号，迟到的结果不会新建会话或覆盖已提交答题记录。
- 会员弹窗打开时隐藏当前页面的自定义底部导航，关闭、离页或卸载后恢复；保留安全区与滚动面板。

## 验证

- 新增完整练习权限、入口拦截、弹窗生命周期、报告页离开取消、账号 A/B 切换回归测试。
- 既有访客离线练习测试按新要求调整为拒绝非随机练习；题库、列表及交互测试明确使用会员授权场景。
- 独立 reviewer 发现的报告重练取消与账号切换两项并发问题均已修正并复核。
- 微信开发者工具实际模拟器截图中，两颗弹窗按钮完整可见；读取到当前 tab bar 的隐藏标志为 `true`。截图保存在本地临时目录，未上传至仓库。关闭与返回页面的恢复行为由组件测试覆盖；后续自动化连接失败，因此未声称完成真机或完整端到端验收。
- 独立 tester 最终全量复验：89 个文件、1,023 项测试全部通过，包含原提交后复习的 12 个集成场景。TypeScript、ESLint、Stylelint、格式、主包预算和 accountSync 构建产物一致性检查均通过。日志位于 `miniapp/tmp/membership-gate-20260923/final-*` 及 `final2-*`。

## 包体与范围

- 主包仍遵守 1.5 MiB 上限。原运行时已过滤掉的 6 条未发布质检实操内容，原样保存在 `miniapp/content/unpublished-inspector-practical-skills.ts`，避免占用主包；当前发布的 6 条仓储实操内容和入口保持一致。
- reviewer 逐对象比较确认拆分前后内容一致；项目结构测试同时验证发布与归档各 6 条素材引用，项目结构和包体检查合计 33 项通过。
- 沿用维护者指定工作树，保留原有未提交修改；本次没有调整支付订单、价格、云函数或线上账户数据。
- 本地 `package.json` 与锁文件根版本均为 1.1.5。

## 开发版本上传

- 沿用维护者此前的上传要求，2026-09-23 通过微信开发者工具官方 CLI 上传开发版本 **1.1.5**，AppID 为 `wx84ecacec08ca162c`。
- CLI 返回 `√ upload`，退出码 0。回执为 `tmp/membership-gate-20260923/upload-1.1.5.json`。
- 总包 1,584,641 字节，主包 1,472,405 字节，辅助包 112,236 字节。
- 上传说明：免费用户每天仅3次随机练习，其他练习需会员；修复会员提示遮挡，保留答题卡与本次错题解析。
- 本次完成开发版本上传，未提审或正式发布；没有重新核验此前审核版本的线上状态。
