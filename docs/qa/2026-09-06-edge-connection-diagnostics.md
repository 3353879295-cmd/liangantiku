# Edge 公众平台连接诊断

日期：2026-09-06。用户明确暂停支付部署及真实交易，本次只检查浏览器连接。保留原标签页、账号登录和此前支付代码/493 项测试成果。

## 扩展重载后的最新结果

用户已在 Edge 扩展管理页停用再启用 ChatGPT 扩展。实时枚举确认 provider 会话标识变化。随后新建 example.com 成功，约 1.6 秒取得完整文本。原公众平台标签页仍在 30 秒超时。

使用工具公开的底层 Browser API 分开执行 `tabs.new()`、`goto()`、`playwright.domSnapshot()`，曾成功读取公众平台首页：账号名称为“粮安题库”，存在管理、虚拟支付、开发管理与云服务入口，证明当时登录有效。点击虚拟支付入口、在新标签页直接导航虚拟支付地址仍超时；后续首页导航也再次超时。因此只能记为部分恢复，不能宣称连接已稳定解决，也不能断言故障仅发生在旧标签页。

读取了工具已公开的 CDP capability 使用说明，尚未执行任何 CDP 命令或修改浏览器行为。失败调用触发工具 JavaScript 会话重置，会干扰后续附着状态判断；没有足够证据确定最初根因。

决定暂停自动浏览器操作并使用用户允许的人工页面截图核验。下一步只请求虚拟支付“道具管理”列表截图，以核对商品 ID、价格和发布状态。推送设置和云环境随后逐页核验，不把首页可读当作这些项目已经通过。不要求用户再次重载、扩大权限或发送密钥。

## 已执行的最小测试

| 检查 | 结果 | 能说明什么 |
| --- | --- | --- |
| 枚举浏览器与标签页 | Edge，配置文件 cheng，扩展通道，可看到公众平台和搜索页 | 扩展枚举通道可用；不代表页面控制可用 |
| 同一 Edge 新建 example.com 并读取 | 成功，返回 Example Domain 标题、正文和链接 | 本次开始时可以新建、导航及读取普通网页 |
| 原公众平台 getTab | 在公开网页通过后进行一次对照测试，45 秒超时 | 原标签页无法读取，不能据此认定账号失效 |
| 同一 Edge 新建公众平台根地址 | 创建后跳转到后台首页；读取返回 Detached while handling command | 已有会话跳转迹象，但正文/登录状态未核验 |
| 改用工具公开的截图接口 | 新公众平台页返回 Debugger unattached | 失败涉及调试器附着，而非仅可访问性文本树 |
| 对公开测试页读取 DOM | 同样返回 Debugger unattached | 此时问题已经影响公开页，不能归因于公众平台独有页面内容 |
| 使用扩展 claimTab 接管公开测试页 | 20 秒超时 | 已尝试不同的受支持恢复接口；没有重复循环 getTab |
| Codex 内置浏览器公开页测试 | 20 秒超时 | 当前尚未验证出可用的内置浏览器替代路径 |

浏览器工具执行超时会重置该工具的 JavaScript 会话，因此后续调试器脱离可能受重置影响；不能把这些现象单独当成最初超时的根因。

## 当前结论与下一步

已定位失败环节为页面控制/调试器附着。浏览器扩展消息通道并非完全失联。尚不能证明初始根因是另一个调试器占用、扩展暂停、任务会话卡住或页面自身行为；没有登录失效证据。

scout 只读定位 `C:\Users\Administrator\AppData\Local\Codex\Logs\2026\09\06` 后发现以下日志证据（北京时间）：14:54:22 多次 `No ChatGPT browser route is available`，随后 IAB 路由及 webview attach 成功；14:54:23 页面 `dom-ready`；14:55:19 浏览器 host 创建后随即记录 route dispose/session activity end。日志支持 Codex IAB 路由反复不可用/重建，尚不足以归因 Edge 首次超时。未找到直接记录 Edge `Debugger unattached` 原因的日志；不把其他任务的会话元数据异常认定为本任务根因。

已请求用户只打开 Edge 的扩展面板并反馈状态，不点击停止/断开或修改权限。当前工具没有可用的原生窗口控制入口，无法自行读取 Edge 工具栏扩展弹窗。收到实际状态后再选择一个必要动作，并复测公开页及公众平台。

后续用户截图确认：扩展在公众平台允许使用；侧栏能显示模型及输入框；扩展菜单只有 App settings / Edge computer use settings。用户随后自行将审批、历史记录、下载和上传设置为始终允许，设置页没有网站专属限制，完整 CDP 访问已开启。权限变化后的公开页新建测试仍在 30 秒超时，未继续反复读取公众平台。此结果说明权限调整未恢复控制，不证明需要进一步放宽权限。

重载前，tester 独立工具会话可以枚举同一 Edge 配置文件，但公开页新建仍在 30 秒超时，没有取得可读页面。此后已由用户重载扩展，结果见本文开头；不能记为已完全修复。

若自动控制仍不能恢复，可由用户在公众平台逐页提供非敏感字段，代理负责核对商品 ID/价格/发布状态、推送 URL/模式及云环境名称/运行配置。实际密钥只在平台配置，不发送到聊天；该人工核验也不等于真实支付通过。

未清除浏览器数据、退出账号、重装浏览器、改网络或重启 Edge；未部署支付或发起交易。

参考：[官方浏览器扩展故障排查与入口说明](https://learn.chatgpt.com/docs/chrome-extension)。本次遵守用户约束，没有执行官方通用建议中的浏览器重启或重新安装。
