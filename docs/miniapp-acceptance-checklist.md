# 小程序 UI 重设计验收清单

验收基线：2026-07-26 UI 重设计实施计划。`PASS` 只表示对应证据真实执行并通过；`PARTIAL` 表示自动化契约通过但仍缺少开发者工具或真机观察；`INCONCLUSIVE/TIMED OUT` 表示命令已执行但没有产生可判定结果；`NOT RUN` 表示未执行。后两者都不视为通过。

自动化实现验证已经完成；依赖外部开发者工具 GUI 和设备的视觉验收仍待完成，因此整体验收状态保持 `PARTIAL`。

## 最终验收

| 状态    | 验收项                                                                 | 本轮证据                                                                                                                            |
| ------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| PASS    | 底部导航仅为“首页、实操、我的”                                         | 工程结构测试断言 `home/practical/profile` 三项且注册页面完整。                                                                      |
| PARTIAL | 首页完成题库合并、两阶段职业等级选择、五个刷题入口、目录摘要和最近记录 | Presenter、路由与结构自动化通过；开发者工具内的布局和点击链路 `NOT RUN`。                                                           |
| PARTIAL | 两个职业各显示五等级，待补充等级不创建空练习                           | 可用性与真实题量门禁自动化通过；模拟器点击 `NOT RUN`。                                                                              |
| PARTIAL | 教材目录跟随当前职业等级，章节号不带前导零                             | 目录 Presenter 与结构自动化通过；模拟器排版观察 `NOT RUN`。                                                                         |
| PARTIAL | 选项视觉、`28rpx`/`1.5`/`600` 字体、四状态和动效符合规格               | `96rpx` 高度、`34rpx` 圆角、`28rpx`/`600`/`1.5` 与状态机契约通过；日间/夜间实际渲染与点击动效 `NOT RUN`。                           |
| PASS    | 模拟考试交卷前不显示答案和解析                                         | 会话、选项与页面契约自动化通过。                                                                                                    |
| PARTIAL | 夜间模式持久化并覆盖答题、答题卡、结果、解析、错题/收藏页面            | ThemeService 与页面接线自动化通过；关闭重开后的 GUI 观察 `NOT RUN`。                                                                |
| PARTIAL | 收藏可反复切换，反馈在 1.6 秒后隐藏                                    | 收藏服务与 Toast 定时器自动化通过；真实动效 `NOT RUN`。                                                                             |
| PARTIAL | 实操使用语义图标和本地详情照片                                         | 静态 WXML `src`、当前 12 个 `iconAsset` 和 4 个可选头像均为本地现存文件；开发者工具渲染 `NOT RUN`，技能到图标的逐项语义映射未锁定。 |
| PARTIAL | “我的”无重复复习板块，个人中心二级页完整                               | 页面结构和服务接线自动化通过；二级页 GUI 返回链路 `NOT RUN`。                                                                       |
| PASS    | 旧学习记录、题目 ID、目录 ID 和收藏/错题数据无损                       | `npm run sync:questions` 后生成题库与目录相对 Git 为零差异，迁移和服务测试通过。                                                    |
| PARTIAL | 全量自动验证、题库同步验证和微信开发者工具烟测全部通过                 | `npm run verify`、同步后测试和 `git diff --check` 通过；微信开发者工具烟测 `NOT RUN`。                                              |

## 自动化证据

- [x] `npm run verify`：TypeScript、ESLint、Stylelint、Prettier 通过；Vitest 21 个测试文件、171 个测试通过。
- [x] `npm run sync:questions`：六个分片分别生成 20、8、9、8、8、8 道题。
- [x] 同步后 `npm run test`：21 个测试文件、163 个测试通过。
- [x] 同步后题库和目录生成物 `git diff --exit-code` 为 0；未发现题目 ID、章节 ID 或职业编码漂移。
- [x] `git diff --check` 为 0。
- [x] 结构测试覆盖 16 个注册页面四件套、递归注册的本地组件四件套、三主 tab、所有静态 WXML 图片 `src`，以及当前 12 个实操 `iconAsset` 和 4 个可选头像。
- [x] 图片契约拒绝 `http:`、`https:`、`//`、`data:`、`blob:` 及其他非本地 scheme；静态本地路径必须可解析为文件。动态持久化字段（例如历史 `avatarUrl`）不由结构扫描泛化保证。
- [x] 选项样式契约锁定 `min-height: 96rpx`、`border-radius: 34rpx`、正文 `28rpx`、字重 `600` 和行高 `1.5`。

## 微信开发者工具

工具路径：`C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat`，可读取帮助，程序版本显示 `1.03.0`。

- `INCONCLUSIVE/TIMED OUT` 登录态确认：`cli.bat islogin --lang zh` 已执行，但在 34 秒内无输出并超时，不能判定登录状态。
- `INCONCLUSIVE/TIMED OUT` 项目打开：单次 `cli.bat open --project <仓库>\miniapp --lang zh --disable-gpu` 已执行，但在 64 秒内无输出并超时；结束后无开发者工具进程，也没有工程文件变更，不能推断编译结果。
- [ ] `NOT RUN` “工具 → 构建 npm”：CLI 服务未能启动，未继续调用 `build-npm`，避免反复驱动 GUI。
- [ ] `NOT RUN` 控制台无编译错误、无未处理 Promise：没有可用的开发者工具会话。
- [ ] `NOT RUN` 日间/夜间完整烟测：没有可用的模拟器或 GUI 自动化会话。

手工验收：登录微信开发者工具，导入仓库 `miniapp/`；执行“工具 → 构建 npm”和“编译”；确认控制台无错误后，严格按 `docs/qa/2026-07-26-ui-redesign-smoke-test.md` 的模拟器清单逐项操作并记录机型、基础库版本和截图。

## 真机

- [ ] `NOT RUN` iOS：本轮没有连接或提供 iOS 真机。
- [ ] `NOT RUN` Android：本轮没有连接或提供 Android 真机。
- [ ] `NOT RUN` 日间/夜间对比度、触控高度、安全区和大字号：必须在至少一台 iOS 与一台 Android 上逐项观察。

手工验收：在 iOS 和 Android 各预览一次，使用长题干与系统大字号检查 `28rpx`、`line-height: 1.5`、`font-weight: 600`、触控高度和安全区。若低端 Android 字形粘连，只在 Android 样式分支降为 `500`，并记录机型和截图；不得降为 `400`。

## 已知未解决关注项

- 实操结构测试允许规定的本地素材，但尚未锁定每个技能到图标的逐项语义映射。
- 薄弱章节在指标完全相同时使用字典序而非数值序打破平局。
